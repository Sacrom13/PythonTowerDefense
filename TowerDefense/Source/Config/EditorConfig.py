# Editor Configurations
EditorConfigs = {
                    'TileDimensions' : (30, 30),            # Dimensions of tiles
                    'TileSelectionOffset' : (20,30),        # Offset of TileSelection within sidebar
                    'TilesPerRow' : 2                       # Amount of tiles to draw per row in selection sidebar
                }  